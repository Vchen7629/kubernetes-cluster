---
apiVersion: v1
kind: ConfigMap
metadata:
  name: airflow-init-schema
  namespace: postgres
data:
  init.sql: |-
    -- Create Airflow metadata database and user;
    CREATE USER airflow_user WITH PASSWORD 'adskjkaddwasd-2323!!';
    REVOKE ALL ON DATABASE airflow FROM cyphria_user;

    CREATE DATABASE airflow OWNER airflow_user
