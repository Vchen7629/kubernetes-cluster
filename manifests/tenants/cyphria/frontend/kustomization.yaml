---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Application related resources
  - application/deployment.yaml
  - application/configmap.yaml
  - application/service.yaml
  - application/autoscaler.yaml

  # application routes
  - routes/ingress.yaml

  # network policies (egress)
  - network-policy/egress-to-argocd-repo-server.yaml
  - network-policy/egress-to-insights-api.yaml
  - network-policy/egress-to-kubedns.yaml

images:
  - name: ghcr.io/vchen7629/cyphria-frontend
    newTag: frontend-1.0.3
