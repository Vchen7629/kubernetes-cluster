apiVersion: gateway.networking.k8s.io/v1
kind: GRPCRoute
metadata:
  name: conversation-briefing-grpc
spec:
  parentRefs:
  - name: mosaic-gateway
  hostnames:
  - "conversation-briefing.mosaic.com"
  rules:
  - backendRefs:
    - name: conversation-briefing-service
      port: 30030