apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgres-crds
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Vchen7629/kubernetes-cluster.git
    targetRevision: main
    path: manifests/data/postgres

  destination:
    server: https://kubernetes.default.svc
    namespace: postgres
  
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true