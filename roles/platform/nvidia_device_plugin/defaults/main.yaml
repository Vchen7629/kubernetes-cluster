---
nvidia_device_plugin_vars:
  kubernetes:
    helm:
      chart:
        name: nvidia-device-plugin
        version: 0.17.0
      repository:
        url: https://nvidia.github.io/k8s-device-plugin
    namespace: nvidia-device-plugin
