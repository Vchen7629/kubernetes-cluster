apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: metrics-server-crds
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Vchen7629/kubernetes-cluster.git
    targetRevision: main
    path: manifests/observability/metrics_server

  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true