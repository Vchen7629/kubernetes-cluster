- name: Deploy ranking calculator crds (deployment, service, network-policies)
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('ansible.builtin.template', 'deploy_crds.yaml') | from_yaml }}"
    kubeconfig: /etc/rancher/k3s/k3s.yaml