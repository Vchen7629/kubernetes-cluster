---
- name: Deploy insights api crds (deployment, service, hpa, network-policies)
  kubernetes.core.k8s:
    state: present
    definition: "{{ lookup('ansible.builtin.template', 'deploy_crds') | from_yaml }}"
    kubeconfig: /etc/rancher/k3s/k3s.yaml
