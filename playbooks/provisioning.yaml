---
- name: OS configuration
  hosts: cluster
  become: true
  gather_facts: true
  roles:
    - bootstrap/cluster
  tags: cluster

#- name: Provision Load Balancer
#  hosts: cluster
#  become: true
#  gather_facts: true

- name: K3s Provisioning
  hosts: cluster
  become: true
  gather_facts: true
  roles:
    - bootstrap/k3s
    - bootstrap/helm
  serial:
    - 1
    - 2
    - 5
  tags: kubernetes

- name: ArgoCD deployed cluster applications provisioning
  hosts: server
  become: true
  gather_facts: true
  post_tasks:
    - name: Perform post-install tasks
      ansible.builtin.include_role:
        name: "{{ postinstall }}"
        tasks_from: postinstall
      loop:
        - bootstrap/argocd
        - observability/metrics_server
        - networking/cilium
      loop_control:
        loop_var: postinstall
  roles:
    - networking/cilium
    - bootstrap/argocd
    - observability/victoria_metrics
    - platform/cert_manager
    - observability/metrics_server
    - networking/cloudflare_tunnel
    - networking/external_dns
    - data/postgres
    - data/valkey
  tags: charts

- name: ArgoCD deployed cyphria tenant applications provisioning
  hosts: server
  become: true
  gather_facts: true
  roles:
    - tenants/cyphria/frontend
    - tenants/cyphria/insights_api
    - tenants/cyphria/data_pipeline_ingestion
    - tenants/cyphria/data_pipeline_llm_summary
    - tenants/cyphria/data_pipeline_ranking_calculator
    - tenants/cyphria/data_pipeline_sentiment_analysis
    - platform/airflow
  tags: cyphria
